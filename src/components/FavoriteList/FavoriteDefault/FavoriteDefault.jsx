import { Defaults, Container, Title } from './FavoriteDefault.styled';

export const Default = () => {
  return (
    <Container>
      <Title> YOUR FAVORITE LIST IS EMPTY</Title>
      <Defaults />
    </Container>
  );
};
